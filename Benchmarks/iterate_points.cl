__kernel void iterate_points(
    __global float4* cReals,
    __global float4* cImags,
    int maxIterations,
    __global int* iterations)
{
    // Initialize z

    // Initialize z2

    // Initialize iterations/increment

    //do
    //{

    //} while ()
}